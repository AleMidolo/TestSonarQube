final void addLineNumber(final int lineNumber) {
    if (lineNumber <= 0) {
        throw new IllegalArgumentException("lineNumber must be strictly positive");
    }
    // Logic to add the line number corresponding to the label
    // This could involve storing the line number in a data structure
    // For example, a list or a map could be used to associate the line number with the label
}