def __init__(self, elements: List[str]):
    """
    Initialize the CombinationCalculator with a list of elements.
    :param elements: List of string elements to generate combinations from.
    """
    self.elements = elements