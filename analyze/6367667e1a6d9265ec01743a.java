public long lastWriteTimeStampInMilliseconds() {
    // Assuming there's a variable to hold the last write timestamp
    long lastWriteTimestamp = System.currentTimeMillis(); // Example of getting the current time
    return lastWriteTimestamp;
}